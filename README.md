# Tool Box

로컬에서 `npm run dev`로 실행할 수 있는 탭형 툴박스입니다.

## 기능

- 상단 탭 기반 UI
- QR Maker & Reader + 간단한 생성 벤치마킹
- 텍스트 글자 수 / UTF-8 바이트 수 / 줄 수 / 공백 제외 글자 수 확인
- JSON Pretty Viewer

## 실행 방법

```bash
npm run dev
```

기본 포트는 `3000`이며, 브라우저에서 `http://127.0.0.1:3000`으로 접속하면 됩니다.

## 참고

QR 생성과 리딩은 브라우저에서 CDN으로 로드한 라이브러리를 사용합니다.
